spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/internet_bank_db
    username: postgres
    password: root
  jpa:
    show-sql: true
    generate-ddl: false
    hibernate.ddl-auto: update
    properties:
      hibernate.jdbc.lob.non_contextual_creation: true

logging:
  file:
    name: application.log
